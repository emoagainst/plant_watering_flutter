import 'package:flutter/material.dart';

class WateringCalendar extends StatelessWidget {

  const WateringCalendar({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Calendar');
  }
}

